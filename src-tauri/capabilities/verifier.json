{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "verifier",
  "description": "Bartleby verification window capabilities",
  "windows": ["verifier"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
